{% extends 'base.html.twig' %}

{% block title %}Upload{% endblock %}

{% block body %}
    <div class="mx-auto pt-20 max-w-4xl flex flex-col">
        {# <form action="" method="post" name="form" class="flex flex-col justify-end" enctype="multipart/form-data"> #}
        {{ form_start(form) }}
            {% include 'components/dropzone.html.twig' %}
            {% include 'components/textinput.html.twig' with {'placeholder': 'Titre'} %}
            {% include 'components/textarea.html.twig' with {'placeholder': 'Description'} %}
            {% include 'components/select.html.twig' with {'options': categories} %}
            <div class="flex justify-end my-8">
                {% include 'components/submit.html.twig' with {'value': 'Upload'} %}
            </div>
        {{ form_end(form) }}
        {# </form> #}
        {# {{ form_start(form) }}
            <div class="flex items-center justify-center w-full">
                <label for="dropzone-file {{ form.path.vars.id }}_file" class="flex flex-col items-center justify-center w-full h-64 border-2 border-primary border-dashed bg-primary/20 hover:bg-primary/30  rounded-lg cursor-pointer">
                    <div class="flex flex-col items-center justify-center pt-5">
                        <svg aria-hidden="true" class="w-10 h-10 mb-3 dark:text-secondary" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path></svg>
                        <p class="text-sm dark:text-secondary/60"><span class="font-semibold">Sélectionnez votre création</span> ou glissez déposez</p>
                    </div>
                    {{ form_widget(form.path, {'attr': {'class': 'file-input file-input-ghost focus:outline-none'}}) }}
                    <p class="text-xs dark:text-secondary/60">SVG, PNG, JPG or GIF</p>
                </label>
            </div>


            {{ form_widget(form.title, {'attr': {'placeholder': 'Titre', 'class': 'input input-bordered w-full mt-8'}}) }}

            <div class="form-control mt-8">
            {{ form_widget(form.description, {'attr': {'placeholder': 'Description', 'class': 'textarea textarea-bordered'}}) }}
                <label class="label">
                <span class="label-text-alt"></span>
                    <span id="char-count" class="label-text-alt">0/255</span>
                </label>
            </div>

            {{ form_widget(form.category, {'attr': {'class': 'select select-bordered w-full'}}) }}
            
            <div class="flex justify-end my-8">
                {% include 'components/submit.html.twig' with {'value': 'Upload'} %}
            </div>

        {{ form_end(form) }} #}

        {# {{ form_start(form) }}
            {{ form(form) }}
        {{ form_end(form) }} #}

    </div>
{% endblock %}
